import { io } from 'socket.io-client';

// const socket = io('http://localhost:8080');

// export const ioConnect = async () => {
//   await socket.on('connect', () => {
//     console.log(`You connected with ${socket.id}`);
//   });
// };
